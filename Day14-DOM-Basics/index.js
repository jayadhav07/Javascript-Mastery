// Day 14 - DOM Basics (this file contains code for browser console)
// Example: create elements and append in browser
/* 
const ul = document.createElement('ul');
document.body.appendChild(ul);
['a','b','c'].forEach(t => {
  const li = document.createElement('li');
  li.textContent = t;
  ul.appendChild(li);
});
*/
console.log('DOM snippets are in comments for browser execution.');
